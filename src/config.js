export const API_URL = "http://localhost:8181";   //Note: Make sure to replace the URL both before and after the colon(:) to avoid compile time errors. THAT LINKS USED ARE SERVER-SIDE LINKS
console.log("API_URL :", API_URL ); // Log the API URL for debugging